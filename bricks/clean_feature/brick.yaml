name: clean_feature
description: A brick that generates a clean feature

repository: https://github.com/n0ks/clean_feature
version: 0.1.5+1
environment:
  mason: ">=0.1.0-dev.50 <0.1.0"

vars:
  feature:
    type: string
    description: Feature name
    default: Todo
    prompt: Write the feature name
